name: revised_flashcard_application
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  sqflite: ^2.4.1
  path: ^1.9.0
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  flutter_tts: ^4.2.3
  amplify_flutter: ^2.6.5
  amplify_auth_cognito: ^2.6.5
  amplify_authenticator: ^2.3.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
